import React from 'react';
import ReactDOM from 'react-dom';

// RAN OUT OF TIME!

it('renders the component', () => {
  // to do
});

it('renders 3 train times', () => {
  // to do
});

it('sets nextTrains data', () => {
  // to do
});

it('updates time since last train', () => {
  // to do
});

